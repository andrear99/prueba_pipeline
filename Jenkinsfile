pipeline {
    agent any
    stages {
        stage("Compilar") {
            steps {
                //sh "cd src"
                //sh "cd main"
                //sh "cd jenkins"
                javac holamundo.java
                java holamundo.

            }
        }
        stage("Probar") {
            steps {
                echo 'No hay tests'
            }
        }
    }
}
